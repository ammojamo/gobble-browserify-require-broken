{
  "name": "gobble-browserify-require-broken",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gobble": "^0.10.2",
    "gobble-browserify": "^0.6.1"
  }
}
